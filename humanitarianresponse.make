; Makefile for Humanitarianresponse Production Sites

includes[common] = "common.make"

includes[hr] = "hr.make"

includes[crf] = "crf.make"

projects[message_library][location] = http://features.humanitarianresponse.info/fserver
